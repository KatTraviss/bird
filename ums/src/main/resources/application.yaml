## Server
server:
  port: 9000

## H2 Database (in-memory for testing)
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true

## MySQL (commented out)
# spring:
#   datasource:
#     driverClassName: com.mysql.cj.jdbc.Driver
#     url: jdbc:mysql://0.0.0.0:3306/ums?serverTimezone=UTC&useLegacyDatetimeCode=false
#     username: root
#     password: passw

## GitHub OAuth2
spring:
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23limcolzShcNmvpSX
            client-secret: 8844f618fff7bbf63fd8dedd1dedd823977188fb
            scope: user:email
